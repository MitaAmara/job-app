<nav class="navbar navbar-expand-lg navbar-dark bg-info">
 
    <a class="navbar-brand" href="#">| Jobs App |</a>
 
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
 
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
 
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <% if current_user %>
            <%= link_to "Dashboard", works_path, :class => 'nav-link' %>
          <% else %>
            <%= link_to "Home", root_path, :class => 'nav-link' %>
          <% end %>
        </li>
        <li class="nav-item active">
          <% if current_user %>
            <%= link_to "Profile", edit_user_path(current_user), :class => 'nav-link' %>
          <% end %>
        </li>
        <li class="nav-item active">
          <% if current_user %>
            <%= link_to "Job", works_path, :class => 'nav-link' %>
          <% end %>
        </li>
      </ul>
      <% if current_user %>
        <span class="navbar-text mr-3" style="color: white">
        Logged in as <%= current_user.email %>
        </span>
        <%= link_to "Log Out", session_path("current"), method: "delete", :class => 'btn btn-light' %>
      <% else %>
        <%= link_to "Registration", new_user_path, :class => 'btn btn-light mr-2' %>
        <%= link_to "Sign In", new_session_path, :class => 'btn btn-light' %>
      <% end %>
    </div>
 
</nav>